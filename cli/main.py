from indexing.index_pipeline import run_indexing
from demos.demo_agent import run_agent_demo_interactive
from demos.demo_chain import run_chain_demo

if __name__ == "__main__":
    # 1) Solo la primera vez
    # run_indexing()

    # 2) Demo agentic (interactiva)
    #run_agent_demo_interactive()

    # 3) Demo chain (opcional)
    run_chain_demo()

